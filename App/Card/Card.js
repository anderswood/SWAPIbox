import React from 'react';
import PropTypes from 'prop-types';

const Card = ( {cardArr} ) => {
  console.log(cardArr);
  return(
    <div></div>
  )


}

Card.propTypes = {
  cardArr: PropTypes.array
}

export default Card
